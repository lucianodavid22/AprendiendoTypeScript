import { Person, gerenateRandomNumber } from './myModules';
import { PI as tresComaCatorce } from './myModules';
import * as myCode from './myModules';

console.log(tresComaCatorce);

const user: Person = {
    id: 22,
    name: 'Luciano'
};

console.log(user);

console.log(myCode.gerenateRandomNumber());